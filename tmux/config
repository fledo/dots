# Rebind prefix from ctrl-b to ctrl-a
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1

# Mouse scroll window/pane
set -g mode-mouse on
